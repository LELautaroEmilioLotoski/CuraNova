
export enum AppointmentStatus {
  SCHEDULED = "scheduled", //Turno programado
  CANCELLED = "cancelled", //Turno cancelado
  COMPLETED = "completed", //Turno completado
  NO_SHOW = "no_show" //Paciente que no assistio
}